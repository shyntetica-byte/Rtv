{
  "entities": {
    "snippets": {
      "title": "Code Snippet",
      "description": "A shared Pine Script code snippet",
      "type": "object",
      "properties": {
        "code": {
          "type": "string",
          "description": "The Pine Script source code"
        },
        "language": {
          "type": "string",
          "description": "The language of the snippet (e.g., pine)"
        },
        "title": {
          "type": "string",
          "description": "Optional title for the snippet"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the snippet was created"
        },
        "authorId": {
          "type": "string",
          "description": "The UID of the user who created the snippet (if any)"
        }
      },
      "required": ["code", "language", "createdAt"]
    }
  },
  "firestore": {
    "/snippets/{snippetId}": {
      "schema": "snippets",
      "description": "Publicly accessible shared snippets (read-only for non-authors)"
    }
  }
}
